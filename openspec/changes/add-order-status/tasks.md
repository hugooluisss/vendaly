# Tasks

## 1. Backend: schema and migration

- [x] 1.1 Write a Cycle migration adding `next_order_number INTEGER NOT NULL DEFAULT 1` to `businesses`; `order_number INTEGER NULL`, `status_id BIGINT NULL REFERENCES order_statuses(id) ON DELETE RESTRICT` to `orders`; and a new `order_statuses` table (`id BIGSERIAL PRIMARY KEY, business_id BIGINT NOT NULL REFERENCES businesses(id) ON DELETE CASCADE, name VARCHAR(255) NOT NULL, color VARCHAR(7) NOT NULL, is_terminal BOOLEAN NOT NULL DEFAULT FALSE, is_default BOOLEAN NOT NULL DEFAULT FALSE, position INTEGER NOT NULL DEFAULT 0`). Note table creation order matters (`order_statuses` before the `orders.status_id` FK). Verify with `docker exec docker-api-1 php bin/migrate.php` and confirm the columns/table exist via `psql \d businesses` / `\d orders` / `\d order_statuses`.
- [x] 1.2 In the same or a follow-up migration, backfill per design.md: (a) four seed `order_statuses` rows per existing business ("Creado" default, "Elaborando", "Entregado" terminal, "Cancelado" terminal); (b) `orders.order_number` via a per-business row-number over `created_at, id`; (c) each business's `next_order_number` to `MAX(order_number) + 1` (or 1 if the business has no orders yet); (d) `orders.status_id` for every pre-existing order to that business's seeded "Creado" status. Then add `NOT NULL` to `orders.order_number` and `orders.status_id`. Verify: every business has exactly 4 seeded statuses with correct default/terminal flags; every pre-existing order has a non-null `order_number` (sequential per business, starting at 1) and `status_id` pointing at "Creado".

## 2. Backend: entities and repositories

- [x] 2.1 Add `OrderStatus` Cycle entity (`apps/api/src/Domain/Entity/OrderStatus.php`) mirroring `PaymentMethod.php`, with the extra `color`/`isTerminal`/`isDefault` properties. Add `nextOrderNumber` to `Business.php`; add `orderNumber`/`statusId` to `Order.php`.
- [x] 2.2 Add `OrderStatusRepositoryInterface` and its Cycle implementation, mirroring `PaymentMethodRepositoryInterface`/`CycleProductIngredientRepository`-style pair (find-by-business, plus a way to check "is this status referenced by any order" — e.g. `existsOrderWithStatus(int $statusId): bool` — for the delete guard).
- [x] 2.3 Extend `CycleOrderRepository` (or add a method) to atomically claim the next order number for a business inside the existing `createWithItems` transaction: `UPDATE businesses SET next_order_number = next_order_number + 1 WHERE id = :id RETURNING next_order_number - 1`, assigning the result to the new `Order` before persisting it. Add a Codeception Unit/Functional test asserting two orders created back-to-back for the same business get sequential numbers, and that a different business's numbering starts independently at 1.

## 3. Backend: business owner status management

- [x] 3.1 Extend `BusinessService::create` to seed the four default `OrderStatus` rows for every newly created business (same transaction as the business row, alongside the existing "Efectivo" payment-method seed). Add a Unit test asserting business creation seeds exactly these four with correct default/terminal flags.
- [x] 3.2 Add `BusinessService` methods to add/rename/recolor/reorder a status, and to set a status as default (unsetting the previous default in the same call). Validate `color` matches `^#[0-9A-Fa-f]{6}$`, rejecting otherwise. Add Unit tests: add/rename/recolor/reorder; setting a new default unsets the old one; invalid color format rejected.
- [x] 3.3 Add a `BusinessService` delete method rejecting: deleting the last remaining status, deleting the current default status, deleting a status referenced by ≥1 order (via `OrderStatusRepositoryInterface::existsOrderWithStatus` or equivalent) — each with a distinct `DomainException` message. Add Unit tests for all three rejection cases plus the success case (delete an unused, non-default status while others remain).
- [x] 3.4 Wire status-list CRUD into `BusinessController` (new routes under the business resource, e.g. `/businesses/{businessId}/order-statuses`) and into the dashboard business-read path so the owner's dashboard reflects current state. Verify via a Codeception Functional test covering add/update/delete/set-default through the real endpoints.

## 4. Backend: order creation, status assignment, and history

- [x] 4.1 Extend `OrderService::create` to look up the business's current default `OrderStatus` (`WHERE business_id = ? AND is_default = true`) and assign it (plus the claimed order number from task 2.3) to the new `Order` before persistence. Add a Unit test asserting a newly-created order gets the business's default status and a sequential order number.
- [x] 4.2 Add an `OrderService`/`BusinessService` method to change an order's status: `(userId, businessId, orderId, statusId)`, guarded by the same ownership check as `listForBusiness`, validating the target status belongs to that business and the order belongs to that business. Add Unit tests: valid status change; status from another business rejected; non-owner rejected.
- [x] 4.3 Wire the status-change method into `OrderController` (a new action, e.g. `PATCH /businesses/{businessId}/orders/{orderId}/status`) and extend `OrderController::map()` (apps/api/src/Web/Orders/OrderController.php:41) to include `order_number` and `status` (id, name, color) in each listed order. Add a Codeception Functional test covering both the extended list response and the status-change endpoint.
- [x] 4.4 Update `WhatsAppOrderLink::generate` to include a "Pedido #<n>" line using the order's `orderNumber`, placed near the top of the message. Add a Unit test asserting the message includes the order number, without breaking the existing message-content assertions from prior sessions (re-read the current test file for `WhatsAppOrderLink` before editing — the exact string format matters to existing passing tests).

## 5. Frontend: dashboard status management

- [ ] 5.1 Add TypeScript models for `OrderStatus` (id, name, color, is_terminal, is_default, position) and API service methods (list/add/rename/recolor/reorder/delete/set-default) alongside the existing payment-methods API calls in `apps/web/src/app/dashboard/business.models.ts`/`business-api.service.ts` (or wherever the payment-methods calls from the prior session live).
- [ ] 5.2 Add a status-list editor to `dashboard/business-settings` (or a new dedicated section), alongside the existing payment-methods editor: add/rename/recolor (a color picker input)/reorder/delete, a way to mark one as default (radio-style, since only one can be default) and toggle terminal (checkbox), with client-side guards mirroring the backend (last-one, current-default, and — best-effort, the server is the source of truth — in-use) and the backend rejection surfaced if raced. Verify by running the app: add a status, set it default, confirm the old default is unset; attempt to delete the current default and confirm it's blocked.
- [ ] 5.3 Add/update a spec covering: status CRUD round-trip, setting default unsets the previous one, delete guards for last/default/in-use (the in-use case can be asserted purely via the surfaced server error message, no need to duplicate server-side "in use" detection client-side).

## 6. Frontend: order history

- [x] 6.1 Extend `apps/web/src/app/dashboard/order-history/order-history.component.ts` (and its models) to read `order_number` and `status` (name + color) from the extended list response, and display them per order (a numbered header per order card, and a colored status badge).
- [x] 6.2 Add a status-change control (e.g. a dropdown of the business's current statuses) per order in `order-history`, calling the new status-change endpoint and updating the displayed status on success, surfacing an error on failure. Verify by running the app: change an order's status from the history view and confirm it persists on reload.
- [x] 6.3 Add/update `order-history.component.spec.ts` covering: order number and status badge render correctly, changing status calls the API and updates the view, an API error surfaces without losing the previous displayed status.

## 7. Verification

- [ ] 7.1 Run the full backend suite (`docker exec docker-api-1 vendor/bin/codecept run Unit` and `Functional`) and confirm no regressions, including all prior fulfillment/payment-method/product-option tests (re-run since `OrderService`/`WhatsAppOrderLink`/`BusinessService`/`CycleOrderRepository` all change again).
- [ ] 7.2 Run the full frontend suite (`npm test -- --watch=false --browsers=ChromeHeadless` from the host, in `apps/web`) and confirm no regressions.
- [ ] 7.3 Manually exercise the end-to-end flow in the browser: place an order as a customer and confirm the WhatsApp message shows "Pedido #1" (or the next sequential number for that business); in the dashboard, confirm the order appears in order-history with its number and default status badge; change its status and confirm the badge/color updates and persists; in business settings, add a custom status, set it default, confirm new orders get it, then attempt to delete the in-use default status and confirm it's blocked with a clear message.

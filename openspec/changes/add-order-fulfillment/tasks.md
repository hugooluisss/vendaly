# Tasks

## 1. Backend: schema and business configuration

- [x] 1.1 Write a Cycle migration adding `pickup_enabled BOOLEAN NOT NULL DEFAULT TRUE`, `delivery_enabled BOOLEAN NOT NULL DEFAULT FALSE`, `dine_in_enabled BOOLEAN NOT NULL DEFAULT FALSE` to `businesses`, and `fulfillment_type VARCHAR(16) NULL`, `delivery_address TEXT NULL`, `delivery_latitude DECIMAL NULL`, `delivery_longitude DECIMAL NULL` to `orders`. Verify with `docker exec docker-api-1 php bin/migrate.php` and confirm the columns exist via `psql \d businesses` / `\d orders`, and that pre-existing rows read `pickup_enabled = true`.
- [x] 1.2 Add the three boolean properties to `apps/api/src/Domain/Entity/Business.php` and the four fulfillment properties to `apps/api/src/Domain/Entity/Order.php`, matching the existing `latitude`/`longitude` column style (non-final entity, annotation-based).
- [x] 1.3 Add a `BusinessService` method (or extend the existing profile-update method) to toggle the three booleans, rejecting a request that would leave all three `false` with a `DomainException`. Add a Codeception Unit test: enable/disable transitions that keep ≥1 enabled succeed; disabling the last enabled one is rejected.
- [x] 1.4 Extend `BusinessService::setPublished` (apps/api/src/Domain/Service/BusinessService.php:131) to also reject publishing when none of the three booleans is `true` (additive to the existing coordinate-required check already in that method — read it first, don't remove the existing check). Add a Unit test covering the new rejection case alongside the existing coordinate one.
- [x] 1.5 Wire the fulfillment-method toggles into `BusinessController`'s profile update action and into `PublicEntityMapper::business`/the dashboard business-read path so the owner's dashboard reflects current state. Verify via a Codeception Functional test.
- [x] 1.6 Write a follow-up Cycle migration adding `pickup_fee DECIMAL NULL`, `delivery_fee DECIMAL NULL`, `dine_in_fee DECIMAL NULL` to `businesses`; a new `payment_methods` table (`id BIGSERIAL PRIMARY KEY, business_id BIGINT NOT NULL REFERENCES businesses(id) ON DELETE CASCADE, name VARCHAR(255) NOT NULL, position INTEGER NOT NULL DEFAULT 0`), mirroring `product_ingredients` exactly; and `fulfillment_fee_snapshot DECIMAL NULL`, `payment_method_id BIGINT NULL REFERENCES payment_methods(id) ON DELETE SET NULL`, `payment_method_snapshot VARCHAR(255) NULL` to `orders`. Include a one-time backfill `INSERT INTO payment_methods (business_id, name, position) SELECT id, 'Efectivo', 0 FROM businesses` for pre-existing businesses. Verify with `docker exec docker-api-1 php bin/migrate.php`, confirm columns/table exist, and confirm every pre-existing business row now has exactly one `payment_methods` row named "Efectivo".
- [x] 1.7 Add a `PaymentMethod` Cycle entity (`apps/api/src/Domain/Entity/PaymentMethod.php`) mirroring `ProductIngredient.php`; add the three fee properties to `Business.php`; add `fulfillmentFeeSnapshot`/`paymentMethodId`/`paymentMethodSnapshot` to `Order.php`.
- [x] 1.8 Add a `PaymentMethodRepositoryInterface` (`src/Domain/Repository/`) and its Cycle implementation (`src/Infrastructure/Cycle/Repository/`), mirroring the `ProductIngredientRepositoryInterface`/`CycleProductIngredientRepository` pair (find-by-business, replace-for-business or add/rename/delete/reorder — match whichever CRUD shape `ProductIngredientRepositoryInterface` already uses for consistency).
- [x] 1.9 Extend `BusinessService::create` to seed one `PaymentMethod` named "Efectivo" for every newly created business (same transaction as the business row). Add `BusinessService` methods to add/rename/reorder/delete payment methods, rejecting a delete that would leave zero remaining, with a `DomainException`. Add fee inputs (`pickup_fee`/`delivery_fee`/`dine_in_fee`) to the existing fulfillment-toggle update method. Add Codeception Unit tests: business creation seeds "Efectivo"; add/rename/reorder payment methods; deleting the last one is rejected; setting/clearing a fee on a fulfillment method.
- [x] 1.10 Extend `BusinessService::setPublished` to also reject publishing when the business has zero payment methods (should be unreachable in practice given the seed + delete guard, but keep it a real check per design.md). Add a Unit test.
- [x] 1.11 Wire payment-method CRUD and fulfillment fees into `BusinessController` and the dashboard business-read path (`PublicEntityMapper::business` or equivalent) so the owner's dashboard reflects current state. Verify via a Codeception Functional test.

## 2. Backend: public catalog and order intent

- [x] 2.1 Update `CatalogService::publicCatalog` to include the business's enabled fulfillment methods (e.g. `['pickup', 'dine_in']`) in its response. Add a Unit test asserting a published catalog's response lists exactly the currently-enabled methods.
- [x] 2.1b Extend `CatalogService::publicCatalog`'s fulfillment-methods output to include each enabled method's fee (or omit/null when unset), and add the business's payment methods (id + name, in `position` order) to the response. Add/update a Unit test asserting fees and payment methods appear correctly, including the no-fee case.
- [x] 2.2 Extend the `POST /public/orders` request handling and `OrderService` to accept `fulfillment_type` and, for `delivery`, `delivery_address`/`delivery_latitude`/`delivery_longitude`. Validate: `fulfillment_type` is one of the three and is currently enabled for the business; for `delivery`, at least one of address or both coordinates is present. Reject the whole order (no partial creation) on any violation — mirror the existing all-or-nothing option-validation pattern in `OrderService`. Add Codeception Unit tests for: valid pickup order, valid delivery with address only, valid delivery with pin only, delivery with neither rejected, disabled/unknown method rejected, missing method rejected.
- [x] 2.2b Extend the same request handling and `OrderService` to also accept and validate `payment_method_id`: reject if missing or if it doesn't currently belong to the business. Add Unit tests: valid payment method accepted; unknown/foreign payment method rejected; missing payment method rejected.
- [x] 2.3 Persist the validated fulfillment fields on the created `Order`. Add a Unit test asserting the persisted order reflects the submitted fulfillment type and delivery details.
- [x] 2.3b Persist `fulfillment_fee_snapshot` (the method's current fee, or null), `payment_method_id`, and `payment_method_snapshot` (the method's current name) on the created `Order`, and compute `Order.total` as items subtotal plus the fee (0 if none). Add a Unit test asserting a fee-bearing method's total is subtotal-plus-fee, and a fee-less method's total equals the subtotal.
- [x] 2.4 Update `WhatsAppOrderLink::generate` to append a human-readable fulfillment-method line (e.g. "Recolección en tienda", "Entrega a domicilio", "Consumo en el local") and, for delivery, the address and/or a `https://www.google.com/maps?q=lat,lng` link. Add a Unit test asserting message formatting for each of the three methods and for delivery with address-only, pin-only, and both.
- [x] 2.4b Update `WhatsAppOrderLink::generate` to also append: a fee line only when `fulfillment_fee_snapshot` is non-zero, a payment-method line, and a subtotal/fee/total breakdown (subtotal derived as `total - fee`) — keep the existing no-fee message shape unchanged when there's no fee to show. Add Unit tests for: a fee-bearing order's message includes the fee and breakdown; a fee-less order's message is unchanged from before (no fee line, no separate subtotal line); the payment method always appears.

## 3. Frontend: dashboard business settings

- [x] 3.1 Extend `apps/web/src/app/dashboard/business.models.ts` (or equivalent) and the business API service with the three fulfillment-method fields on read/update.
- [x] 3.1b Extend the same models/service with the three fulfillment-fee fields and the payment-methods list (read + add/rename/reorder/delete calls).
- [x] 3.2 Add fulfillment-method toggles (three checkboxes/switches: pickup, delivery, dine-in) to `dashboard/business-settings`, disabling the toggle for the last currently-enabled method in the UI (mirrors the backend rule) with a clear message, and surfacing the backend's rejection if it's raced. Verify by running the app, toggling methods, saving, and confirming persistence on reload.
- [x] 3.2b Add a fee input next to each fulfillment-method toggle (shown/enabled only when that method is on), and a payment-methods list editor (add/rename/reorder/delete), following the same add/remove/reorder UI pattern already used for product ingredients in `dashboard/catalog-management`, with the last-remaining-entry delete disabled client-side (mirroring the fulfillment-method guard) and the backend rejection surfaced if raced. Verify by running the app: set a delivery fee, save, reload, confirm it persisted; add/rename/delete payment methods and confirm the last one can't be deleted.
- [x] 3.3 Add/update a spec covering: toggling methods, and the UI preventing (or the save flow surfacing rejection for) disabling the last enabled one.
- [x] 3.3b Add/update a spec covering: fee input round-trip, and payment-method add/rename/reorder/delete including the last-one-blocked case.

## 4. Frontend: public order flow

- [x] 4.1 Extend `apps/web/src/app/public/public.models.ts` with the enabled-fulfillment-methods field from `GET /public/catalog/:slug`.
- [x] 4.1b Extend the same model with each fulfillment method's fee and with the catalog's payment-methods list.
- [x] 4.2 Add a fulfillment-method selector to `public/order-summary` (or wherever the final submit step lives), offering only the catalog's currently-enabled methods. When `delivery` is selected, show an address text field and a map-pin picker reusing `apps/web/src/app/shared/map/map.component.ts` in point-picking mode (see how `dashboard/business-settings` already uses it for business coordinates). Verify by running the app: a catalog with only `pickup` enabled shows no delivery/dine-in options; a catalog with delivery enabled shows the address/pin fields when delivery is selected.
- [x] 4.2b **Supersedes part of 4.2's placement**: split the flow into two routes. Strip `OrderSummaryComponent` (the existing `/order` route) down to cart-review only — items, quantities, notes, a "Continuar" button navigating to a new route — removing its "Enviar por WhatsApp" button and submit logic entirely. Create a new route (e.g. `/public/catalog/:slug/order/checkout`) and component hosting: the items subtotal, the fulfillment-method selector (from 4.2, moved here) with each option's fee shown, the delivery address/map-pin fields (from 4.2, moved here) when `delivery` is selected, a payment-method selector (radio list from the catalog's payment methods), the resulting fee line (shown only when non-zero) and grand total, and the "Enviar por WhatsApp" button (moved here, now the only one in the flow). Verify by running the app: `/order` has no submit button and a working "Continuar"; the new checkout route shows subtotal/fee/total correctly for a fee-bearing and a fee-less method.
- [x] 4.3 Add a small localStorage-backed helper (or extend `CartService`'s existing try/catch persistence pattern) that remembers the last-submitted delivery address/pin under its own key (e.g. `vendaly.delivery-location`, separate from `vendaly.cart`) and prefills the delivery form with it on load. Add a spec covering: prefill from a stored value, empty form with nothing stored, safe no-op if localStorage throws.
- [x] 4.4 Update the order payload builder to include the selected fulfillment method and delivery details in the `POST /public/orders` call, and block submission client-side until a fulfillment method (and, for delivery, an address or pin) is chosen — mirroring the existing required-option guard pattern. Add/update a spec (`order-payload.spec.ts`) covering the new fields.
- [x] 4.4b Update the order payload builder to also include `payment_method_id`, and block submission client-side (on the new checkout route/component from 4.2b) until a payment method is chosen too. Add/update `order-payload.spec.ts` for the new field.

## 5. Verification

- [x] 5.1 Run the full backend suite (`docker exec docker-api-1 vendor/bin/codecept run Unit` and `Functional`) and confirm no regressions, including the pre-existing coordinate-required-to-publish behavior and the previously-passing fulfillment/delivery tests (re-run after the fee/payment-method additions, since `OrderService`/`WhatsAppOrderLink`/`BusinessService` all change again).
- [x] 5.2 Run the full frontend suite (`npm test`) and confirm no regressions, including the previously-passing cart/order-payload/business-settings specs (re-run after the checkout-route split, since it removes submit logic from `OrderSummaryComponent`).
- [ ] 5.3 Manually exercise the end-to-end flow in the browser: on a business with all three methods enabled (one with a fee, e.g. delivery = $30) and two payment methods, go through cart review → continue → checkout, place a pickup order (no fee line) and a delivery order (fee line, address, map pin, non-default payment method), confirming each WhatsApp message reflects the right method/fee/payment/details and the total math; then confirm the last fulfillment method and last payment method each can't be removed, and catalog publish is blocked if either would reach zero.

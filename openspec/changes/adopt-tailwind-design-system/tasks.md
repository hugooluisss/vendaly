# Tasks

## 1. Foundation: install Tailwind and port brand tokens

- [x] 1.1 Add `tailwindcss`, `@tailwindcss/forms`, `postcss`, `autoprefixer` as dev dependencies in `apps/web/package.json`; if the `web` Docker service is running, recreate its `node_modules` volume per the repo's documented steps. Verify: `npm ls tailwindcss @tailwindcss/forms postcss autoprefixer` in `apps/web` resolves all four with no errors.
- [x] 1.2 Create `apps/web/tailwind.config.js` with `content` globbing `src/**/*.{html,ts}`, `theme.extend.colors` containing every brand token from `src/styles.css` (`vendaly.orange`/`orange-light`/`orange-soft`/`slate`/`muted`/`border`/`border-strong`/`danger`/`shadow`/`shadow-soft`/`surface`/`bg`, `whatsapp.green`/`green-hover`/`text`) with the same hex values, and the `@tailwindcss/forms` plugin registered with `strategy: 'class'`. Verify: `npx tailwindcss -i src/styles.css -o /tmp/out.css --config tailwind.config.js` (or an equivalent one-off build) completes without error and `/tmp/out.css` contains a `bg-vendaly-orange` utility rule with `#ea580c`.
- [x] 1.3 Replace the top of `apps/web/src/styles.css` with the three `@tailwind` directives (`base`, `components`, `utilities`), keep the existing minimal global rules (`* { box-sizing: border-box }`, `html { scroll-behavior: smooth }`, `body { ... }`) below them using the new Tailwind color utilities/theme instead of the raw `:root` custom properties, and remove the now-redundant `:root` block. Verify: `npm run build` (in `apps/web`) succeeds for both the `development` and `gateway` configurations, and `npm start` renders the login screen with the same orange brand color as before (visual check).
- [x] 1.4 Run the full existing Karma suite (`npm test` in `apps/web`, non-watch) before any screen migration and record the baseline (pass/fail count). Verify: baseline recorded; any pre-existing failures are noted so later tasks can distinguish "was already failing" from "broke during migration."
  - **Baseline (recorded before any screen migration):** 82 total specs, 79 passing, 3 failing — all 3 pre-existing in `src/app/auth/auth.interceptor.spec.ts` (`adds the access token to API requests`, `refreshes once after 401 and retries the original request`, `shares one refresh request across concurrent 401 responses`), caused by that spec file hardcoding `http://localhost:8080` URLs while `environment.apiBaseUrl` is `/vendaly-api` since an earlier, unrelated commit — not caused by this change. Note: this sandbox's headless Chrome disconnects Karma after ~30s of wall-clock time regardless of which test is running (an environment quirk, not a hung test), so the full suite must be run in batches by folder (`--include="src/app/<folder>/**/*.spec.ts"`) rather than in one `ng test` invocation to get a complete count.

## 2. Foundation: standardized component classes

- [x] 2.1 Define `.btn`, `.btn-primary`, `.btn-secondary` in `src/styles.css` under `@layer components` using `@apply` over Tailwind utilities, matching the current visual look of `.button`/`.button.secondary` (colors, padding, radius, hover/disabled states) used across the app today. Verify: a throwaway test element using `.btn-primary` in `npm start` renders visually identical to an existing `.button` element side by side, then remove the throwaway element.
- [x] 2.2 Define `.field-input` (text/number/date inputs and textareas) and `.field-select` (pairs with `@tailwindcss/forms`' `form-select` reset) in `@layer components`, sized/bordered consistently with each other. Verify: applying `.field-select` to the broken category `<select>` in `business-settings.component.html` (temporary, ahead of task 3.2) visibly fixes its rendering; revert that temporary edit after confirming, so task 3.2 applies it for real.
- [x] 2.3 Define `.field-checkbox`/`.field-radio` (pairs with `@tailwindcss/forms`' `form-checkbox`/`form-radio` reset) in `@layer components`. Verify: applying to one known-broken checkbox (e.g. a fulfillment-method toggle in `business-settings.component.html`, temporary) visibly fixes its checked/focus state; revert the temporary edit.
- [x] 2.4 Define `.card` in `@layer components` for the card/section container look already used ad hoc (e.g. `.card` class already referenced in several templates today with per-component CSS). Verify: grep confirms `.card` usage exists in templates before this task; after defining it in `styles.css`, remove the duplicate per-component `.card` rule from one component's `.css` file and confirm it still renders the same.

## 3. Dashboard screens

- [x] 3.1 Migrate `dashboard/auth/auth-form` (`auth.component.html`/`.css`): apply `.field-input`, `.btn-primary`, `.card` as applicable; delete superseded rules from `auth.component.css`. Verify: login and register flows still submit correctly (manual check against the local API), and `npm test` shows no new failures beyond the task 1.4 baseline.
- [x] 3.2 Migrate `dashboard/onboarding` (`onboarding.component.html`/`.css`): apply standardized classes; fix the category `<select>` per task 2.2's class. Verify: creating a business via onboarding still works end-to-end; `npm test` shows no new failures.
- [x] 3.3 Migrate `dashboard/business-settings` (`business-settings.component.html`/`.css`) — the screen with the reported broken category select and fulfillment checkboxes: apply `.field-select`/`.field-checkbox`/`.field-radio`/`.field-input`/`.btn-*`/`.card` throughout (profile form, fulfillment fieldset, payment methods, order statuses, hours modal). Verify: saving the profile (including selecting a category) persists correctly on reload (manual check, consistent with the earlier category-save fix); all checkboxes/radios/selects render correctly visually; `npm test` shows no new failures.
- [x] 3.4 Migrate `dashboard/catalog-management` (`catalog-management.component.html`/`.css`): apply standardized classes to product/category forms, list rows, and modals. Verify: creating/editing a product and category still works; `npm test` shows no new failures.
- [x] 3.5 Migrate the `dashboard/dashboard` shell/nav (`dashboard.component.html`/`.css`) and `dashboard/dashboard-home` (`dashboard-home.component.html`/`.css`): apply `.btn-*`/`.card` to nav links and summary cards. Verify: nav highlighting (`routerLinkActive`) still works; `npm test` shows no new failures.
- [x] 3.6 Migrate `dashboard/order-history` (`order-history.component.html`/`.css`): apply standardized classes to the order list/detail views. Verify: order list and status changes still render and function; `npm test` shows no new failures.

## 4. Public screens

- [x] 4.1 Migrate `public/directory` (`public.component.html`, shared `../public.css` portions used only by this screen): apply `.field-input` to the search box, `.card` to business list items, `.btn-*` to category filter chips. Verify: search/filter/infinite-scroll and the map still work; `npm test` shows no new failures.
- [x] 4.2 Migrate `public/catalog-page` (`catalog-page.component.html`, its `../public.css` portions): apply standardized classes to product cards, category nav, and modals. Verify: browsing categories/products and opening the selection modal still works; `npm test` shows no new failures.
- [x] 4.3 Migrate `public/order-summary` and `public/order-checkout` (their `../public.css` portions): apply `.field-input`/`.btn-*`/`.card`; keep the WhatsApp CTA's official green untouched per the spec. Verify: building and sending an order via the WhatsApp deep link still works, and the CTA color is unchanged; `npm test` shows no new failures.
- [x] 4.4 Remove any now-fully-unused rules left in `public/public.css` after 4.1-4.3. Verify: grep for each removed selector confirms no remaining template references it; `npm run build` still succeeds.

## 5. Shared components

- [x] 5.1 Migrate `shared/modal` (`modal.component.html`/`.css`): apply `.card`/`.btn-*` to the modal container/actions. Verify: every screen using `app-modal` (business-settings hours modal, catalog-management modals, catalog-page selection modal) still opens/closes and functions correctly; `npm test` shows no new failures.
- [x] 5.2 Migrate `shared/image-upload` (`image-upload.component.html`/`.css`): apply `.btn-*`/`.field-input` as applicable to its controls. Verify: logo/cover image cropping and upload still works on business-settings; `npm test` shows no new failures.
- [x] 5.3 Migrate `shared/qr-code` (`qr-code.component.html`/`.css`): apply `.btn-*`/`.card` as applicable. Verify: QR generation/download still works on dashboard-home and business-settings; `npm test` shows no new failures.
- [x] 5.4 Review `shared/map` (`map.component.html`/`.css`): apply standardized classes only to non-Leaflet chrome (e.g. surrounding buttons/card), leaving Leaflet's own rendering untouched. Verify: pin placement/map centering still works on business-settings, catalog-management, directory, and order-checkout; `npm test` shows no new failures.
- [x] 5.5 Remove the `:root { --vendaly-*; --whatsapp-*; }` block from `apps/web/src/styles.css` (see design.md's Risks — it was restored during task 1.3's follow-up as a bridge so not-yet-migrated screens kept working). First grep the whole `apps/web/src/app` tree for `var(--vendaly` and `var(--whatsapp` to confirm zero references remain (every screen's migration task, 3.1-5.4, should have converted its own file's usages to standardized classes or `theme()` calls); only delete the block once that grep is empty. Verify: grep is empty, then `npm run build` still succeeds for both configurations.

## 6. Final verification

- [x] 6.1 Run `npm test` (full suite) and `npm run build` (both `development` and `gateway` configurations) one final time. Verify: no new test failures beyond the task 1.4 baseline, and both builds succeed.
- [ ] 6.2 Walk every migrated screen once more end-to-end (dashboard: login/register, onboarding, business settings incl. category select, catalog management, dashboard home, order history; public: directory, catalog page, order summary/checkout) confirming no visually broken select/checkbox/radio remains anywhere. Verify: a written pass/fail note per screen, all passing.
